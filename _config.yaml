title: petrosh
baseurl:
description: Raw minimal theme for Jekyll
permalink: pretty
remote_theme: trasparente/rudra

highlighter: rouge
markdown: kramdown
kramdown:
  auto_ids: true

# Defaults
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
      menu: 0
      comments: false

# Assets
js:
  - beyond-blog
css:
  - default
  - syntax
  - posts

# Custom css
colors:
  - link_color: "#4078c0"
  - grey_color: "#7f7f7f"

# show breadcrumb
show_breadcrumb: true

# Gems
gems:
  - jekyll-coffeescript
